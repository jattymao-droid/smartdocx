<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.education.EduLibraryVipMemberMapper">

    <resultMap id="EduLibraryVipMemberResult" type="EduLibraryVipMember">
        <id property="vipId" column="vip_id"/>
        <result property="username" column="username"/>
        <result property="planCode" column="plan_code"/>
        <result property="status" column="status"/>
        <result property="startTime" column="start_time"/>
        <result property="expireTime" column="expire_time"/>
        <result property="source" column="source"/>
        <result property="orderNo" column="order_no"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectColumns">
        select vip_id, username, plan_code, status, start_time, expire_time, source, order_no,
               remark, create_by, create_time, update_by, update_time
        from edu_library_vip_member
    </sql>

    <select id="selectByUsername" resultMap="EduLibraryVipMemberResult">
        <include refid="selectColumns"/>
        where username = #{username}
    </select>

    <select id="selectVipMemberList" resultMap="EduLibraryVipMemberResult">
        <include refid="selectColumns"/>
        <where>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                and username like concat('%', #{keyword}, '%')
            </if>
        </where>
        order by expire_time desc, vip_id desc
    </select>

    <insert id="insertVipMember" parameterType="EduLibraryVipMember" useGeneratedKeys="true" keyProperty="vipId">
        insert into edu_library_vip_member (
            username, plan_code, status, start_time, expire_time, source, order_no, remark, create_by, create_time
        ) values (
            #{username}, #{planCode}, #{status}, #{startTime}, #{expireTime}, #{source}, #{orderNo}, #{remark}, #{createBy}, CURRENT_TIMESTAMP
        )
    </insert>

    <update id="updateVipMember" parameterType="EduLibraryVipMember">
        update edu_library_vip_member
        <set>
            <if test="planCode != null">plan_code = #{planCode},</if>
            <if test="status != null">status = #{status},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="source != null">source = #{source},</if>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_by = #{updateBy},
            update_time = CURRENT_TIMESTAMP
        </set>
        where vip_id = #{vipId}
    </update>

    <update id="markExpiredBefore">
        update edu_library_vip_member
        set status = '1', update_time = CURRENT_TIMESTAMP
        where status = '0' and expire_time &lt; #{now}
    </update>
</mapper>
